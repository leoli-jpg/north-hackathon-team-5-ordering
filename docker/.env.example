# RFC-0002: Local ordering system environment template for T1.
# Copy this file to docker/.env before running docker compose.
POSTGRES_USER=ordering
POSTGRES_PASSWORD=ordering
POSTGRES_DB=ordering
POSTGRES_PORT=5432
API_PORT=3001
NODE_ENV=development
API_CORS_ORIGIN=http://localhost:3000
